# Install the package itself with: python -m pip install .
numpy>=1.26,<3
PyYAML>=6.0.2,<7
websockets>=14,<16
zeroconf>=0.147,<1
ncnn==1.0.20260526; platform_machine == "aarch64" and sys_platform == "linux"
